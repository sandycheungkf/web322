<!DOCTYPE html>
<html lang="en" data-theme="coffee">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lego Collection - Collection</title>
    <link href="/css/main.css" rel="stylesheet">
</head>

<body tabindex="-1">
    <%- include('partials/navbar', {page: '/lego/sets'}) %>

    <!-- content -->
    <div class="container mx-auto mt-5">
        <div class="hero bg-base-200 rounded-lg p-6">
            <div class="hero-content text-center ">
              <div class="w-md">
                <h1 class="text-5xl font-bold">Collection</h1>
      
                <p class="py-8">Browse the entire collection.  Click the theme to filter by themes such as:               
                    <a href="/lego/sets?theme=supplemental" class="btn btn-xs btn-primary">Supplemental</a>
                    <a href="/lego/sets?theme=playhouse" class="btn btn-xs btn-primary">Playhouse</a>
                    <a href="/lego/sets?theme=town" class="btn btn-xs btn-primary">Town</a>
                </p>
              </div>
            </div>
          </div>
    </div>

    <div class="overflow-x-auto">
        <table class="table">
          <tbody>
                    <% sets.forEach((set) => { %>
      
            <tr>
              <td>
                <div class="flex items-center space-x-3">
                  <div class="avatar">
                    <div class="mask mask-squircle w-12 h-12">
                      <img src="<%= set.img_url %>" alt="Lego Image" >
                    </div>
                  </div>
                </div>
              </td>
                              <td><%= set.name %> | <a href="/lego/sets?theme=<%= set.Theme.name %>" class="btn btn-xs btn-primary "><%= set.Theme.name %></a></td>
                              <td><span class="font-bold">Year: </span><%= set.year %> - <span class="font-bold">Parts: </span><%= set.num_parts %></td>
                              <th><a href="/lego/sets/<%= set.set_num %>" class="btn btn-xs btn-primary">Details</a></th>
      
            </tr>
            <% }); %>
          </tbody>    
        </table>
      </div>
        </div>
    </div>
</body>

</html>
